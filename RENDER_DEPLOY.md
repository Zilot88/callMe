# 🚀 Деплой на Render.com

Полное руководство по развертыванию CallMe на платформе Render.com.

## ✨ Преимущества Render.com

- ✅ **Бесплатный план** - Free tier доступен
- ✅ **Автоматический SSL** - HTTPS сертификаты предоставляются автоматически
- ✅ **Автодеплой из GitHub** - автоматическое развертывание при push
- ✅ **Глобальный CDN** - быстрая доставка контента
- ✅ **WebSocket поддержка** - Socket.IO работает из коробки
- ✅ **Docker поддержка** - используем контейнеризацию

## 📋 Предварительные требования

1. Аккаунт на [Render.com](https://render.com)
2. Проект уже на GitHub: `https://github.com/Zilot88/callMe`

## 🎯 Быстрый деплой (автоматический)

### Способ 1: Через Blueprint (рекомендуется)

1. Зайдите на [Render.com](https://dashboard.render.com)
2. Нажмите **"New +"** → **"Blueprint"**
3. Подключите GitHub репозиторий: `Zilot88/callMe`
4. Render автоматически найдет `render.yaml` и создаст сервис
5. Нажмите **"Apply"**
6. Ждите окончания деплоя (~5-10 минут)
7. Готово! Ваше приложение доступно на `https://callme-xxxx.onrender.com`

### Способ 2: Ручное создание

1. **Зайдите на Render.com Dashboard**
   - https://dashboard.render.com

2. **Создайте новый Web Service**
   - Нажмите **"New +"** → **"Web Service"**

3. **Подключите GitHub репозиторий**
   - Выберите `Zilot88/callMe`
   - Или введите URL: `https://github.com/Zilot88/callMe`

4. **Настройте параметры:**
   - **Name**: `callme` (или любое другое)
   - **Region**: `Oregon` (или ближайший к вам)
   - **Branch**: `master`
   - **Runtime**: `Docker`
   - **Dockerfile Path**: `./Dockerfile.render`

5. **План (Plan):**
   - **Free** - для тестирования (ограничения: усыпает после 15 мин неактивности)
   - **Starter** ($7/мес) - для production (без усыпления)

6. **Environment Variables** (переменные окружения):
   ```
   NODE_ENV=production
   HOSTNAME=0.0.0.0
   ```

   PORT автоматически предоставляется Render (обычно 10000)

7. **Нажмите "Create Web Service"**

8. **Дождитесь завершения деплоя** (~5-10 минут при первом запуске)

## 🔧 Важные настройки

### Auto-Deploy

Render автоматически деплоит при каждом push в `master`:
- ✅ Включено по умолчанию
- Можно отключить в настройках → "Auto-Deploy"

### Health Check

Render проверяет доступность приложения:
- **Path**: `/` (главная страница)
- **Interval**: 30 секунд
- **Timeout**: 10 секунд

### WebSocket Support

WebSocket (Socket.IO) работает из коробки:
- ✅ Никаких дополнительных настроек не требуется
- Render автоматически проксирует WebSocket соединения

## 🌐 После деплоя

### Получение URL

После успешного деплоя ваше приложение будет доступно по адресу:
```
https://callme-xxxx.onrender.com
```

Где `xxxx` - случайная строка. Можно изменить в настройках.

### Кастомный домен (опционально)

1. В настройках сервиса → **"Custom Domains"**
2. Нажмите **"Add Custom Domain"**
3. Введите ваш домен: `callme.yourdomain.com`
4. Настройте CNAME запись в вашем DNS:
   ```
   CNAME callme callme-xxxx.onrender.com
   ```
5. Render автоматически выдаст SSL сертификат (Let's Encrypt)

## 📱 Тестирование

После деплоя протестируйте:

1. **Откройте URL** в браузере:
   ```
   https://callme-xxxx.onrender.com
   ```

2. **Разрешите доступ к камере/микрофону**

3. **Откройте в другой вкладке/устройстве**

4. **Проверьте автоподключение**

### HTTPS автоматический!

Render предоставляет SSL автоматически:
- ✅ Let's Encrypt сертификат
- ✅ Автоматическое обновление
- ✅ Никаких предупреждений в браузере
- ✅ WebRTC работает без проблем на мобильных устройствах

## 🔍 Мониторинг и логи

### Просмотр логов

1. В Dashboard → выберите ваш сервис
2. Вкладка **"Logs"**
3. Логи в реальном времени

Или через CLI:
```bash
# Установите Render CLI
npm install -g @render.com/cli

# Авторизуйтесь
render login

# Просмотр логов
render logs callme
```

### Метрики

В Dashboard → **"Metrics"**:
- CPU использование
- Память
- Запросы
- Время ответа

## 💰 Бесплатный план (Free Tier)

### Ограничения Free плана:

- ⚠️ **Усыпление** - сервис засыпает после 15 минут неактивности
- ⚠️ **Холодный старт** - первый запрос после сна занимает ~30-60 сек
- ✅ **750 часов/месяц** - достаточно для одного сервиса 24/7
- ✅ **100 GB bandwidth** - обычно достаточно
- ✅ **Автоматический SSL** - включен

### Как работает усыпление:

1. Сервис активен когда есть запросы
2. После 15 минут без активности → засыпает
3. При новом запросе → просыпается (~30-60 сек)
4. Подходит для тестирования и демо

### Upgrade на Starter ($7/мес):

- ✅ **Без усыпления** - работает 24/7
- ✅ **Больше ресурсов** - 0.5 CPU, 512 MB RAM
- ✅ **Приоритетная поддержка**

## 🛠 Troubleshooting

### Проблема: Build fails

**Решение:**
1. Проверьте логи сборки
2. Убедитесь что все файлы закоммичены в Git
3. Проверьте `Dockerfile.render` на ошибки

### Проблема: Health check fails

**Решение:**
1. Проверьте что приложение слушает правильный PORT
2. Проверьте логи приложения
3. Убедитесь что `server-render.js` используется

### Проблема: WebSocket не работает

**Решение:**
1. WebSocket должен работать автоматически на Render
2. Проверьте что Socket.IO использует правильный протокол
3. В браузере проверьте Network → WS

### Проблема: Приложение медленно просыпается

**Решение:**
- Это нормально для Free плана
- Upgrade на Starter план для работы 24/7
- Или используйте Cron Job для пинга каждые 10 минут

## 📊 Сравнение планов

| Функция | Free | Starter | Standard |
|---------|------|---------|----------|
| Цена | $0 | $7/мес | $25/мес |
| Усыпление | Да (15 мин) | Нет | Нет |
| CPU | Shared | 0.5 CPU | 1 CPU |
| RAM | 512 MB | 512 MB | 2 GB |
| Bandwidth | 100 GB | 100 GB | 400 GB |
| SSL | ✅ | ✅ | ✅ |

## 🔄 Обновление приложения

### Автоматическое обновление:

```bash
# На вашем компьютере
git add .
git commit -m "Update feature"
git push origin master
```

Render автоматически:
1. Обнаружит новый коммит
2. Соберет новый Docker образ
3. Задеплоит обновление
4. Zero-downtime deployment

### Ручное обновление:

1. Dashboard → выберите сервис
2. Нажмите **"Manual Deploy"** → **"Deploy latest commit"**

## 🔐 Безопасность

Render обеспечивает:
- ✅ Автоматический HTTPS/SSL
- ✅ DDoS защита
- ✅ Изоляция контейнеров
- ✅ Секретное хранение env переменных
- ✅ Автоматические обновления безопасности

### Рекомендации:

1. Не коммитьте секреты в Git
2. Используйте Environment Variables для чувствительных данных
3. Включите GitHub branch protection
4. Регулярно обновляйте зависимости

## 📚 Полезные ссылки

- [Render Documentation](https://render.com/docs)
- [Render Docker Guide](https://render.com/docs/docker)
- [Render WebSocket Guide](https://render.com/docs/web-services#websocket-support)
- [Render Blueprint Spec](https://render.com/docs/blueprint-spec)
- [Render Status Page](https://status.render.com/)

## 🎓 Альтернативы Render

Если Render не подходит, рассмотрите:

1. **Railway.app** - похож на Render
2. **Fly.io** - глобальное развертывание
3. **Heroku** - классика (дороже)
4. **DigitalOcean App Platform** - простой деплой
5. **Vercel/Netlify** - для stateless приложений (не подходит для Socket.IO)

## ✅ Чеклист деплоя

- [ ] Создан аккаунт на Render.com
- [ ] Репозиторий на GitHub обновлен
- [ ] Создан Web Service на Render
- [ ] Выбран Dockerfile.render
- [ ] Настроены Environment Variables
- [ ] Build завершился успешно
- [ ] Health check проходит
- [ ] Приложение доступно по URL
- [ ] HTTPS работает
- [ ] WebRTC/Socket.IO работает
- [ ] Тестирование с мобильных устройств пройдено

---

**Готово!** Ваше приложение CallMe теперь доступно в облаке! 🎉

**Ссылка на приложение:** `https://callme-xxxx.onrender.com`
