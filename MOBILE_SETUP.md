# Настройка для мобильных устройств

## Проблема
WebRTC требует **HTTPS** для доступа к камере и микрофону на мобильных устройствах.
На HTTP это работает только на `localhost`.

## Решение
Мы настроили HTTPS с самоподписанным SSL сертификатом.

## 📱 Как подключиться с телефона

### Шаг 1: Запустите сервер
```bash
npm run dev
```

Вы увидите:
```
> Ready on https://0.0.0.0:4057
> Socket.IO server running with HTTPS
> Local:    https://localhost:4057
> Network:  https://192.168.50.57:4057
> External: https://176.36.188.208:4057

⚠️  WARNING: Using self-signed certificate. You'll need to accept the certificate in your browser.
```

### Шаг 2: Откройте на телефоне

#### Из локальной сети:
```
https://192.168.50.57:4057
```

#### Из интернета:
```
https://176.36.188.208:4057
```

### Шаг 3: Примите сертификат

При первом открытии браузер покажет предупреждение о безопасности:

#### На Android (Chrome/Firefox):
1. Увидите "Ваше подключение не защищено" / "Your connection is not private"
2. Нажмите **"Дополнительно"** / **"Advanced"**
3. Нажмите **"Перейти на сайт..."** / **"Proceed to..."**
4. Готово! Теперь можете разрешить доступ к камере

#### На iOS (Safari):
1. Увидите предупреждение о сертификате
2. Нажмите **"Показать подробности"** / **"Show Details"**
3. Нажмите **"Посетить веб-сайт"** / **"Visit this website"**
4. Подтвердите **"Посетить"** / **"Visit"**
5. Готово! Теперь можете разрешить доступ к камере

### Шаг 4: Разрешите доступ к камере/микрофону

После принятия сертификата браузер запросит разрешение на использование камеры и микрофона:
- Нажмите **"Разрешить"** / **"Allow"**

## 🔒 Безопасность

### Что такое самоподписанный сертификат?
- Это SSL сертификат, который мы создали сами (не от официального центра сертификации)
- Он обеспечивает шифрование, но браузер не может проверить его подлинность
- **Безопасно для локального использования** в вашей сети

### Почему браузер предупреждает?
- Браузер не знает, кто выдал этот сертификат
- Это нормально для локальной разработки
- В production используют сертификаты от Let's Encrypt или других CA

## 🌐 Для публичного доступа (опционально)

Если хотите убрать предупреждения в браузере:

### Вариант 1: Let's Encrypt (бесплатно)
Требует доменное имя:
1. Купите домен (например, `mysite.com`)
2. Направьте его на ваш IP `176.36.188.208`
3. Используйте Certbot для получения бесплатного сертификата

### Вариант 2: Cloudflare Tunnel (бесплатно)
Не требует доменное имя:
1. Зарегистрируйтесь на cloudflare.com
2. Установите `cloudflared`
3. Создайте туннель к вашему серверу
4. Получите бесплатный домен `*.trycloudflare.com` с валидным HTTPS

### Вариант 3: Ngrok (бесплатно с лимитами)
1. Зарегистрируйтесь на ngrok.com
2. Установите ngrok
3. Запустите: `ngrok http 4057`
4. Получите временный домен с HTTPS

## ⚙️ Отключить HTTPS (не рекомендуется)

Если хотите вернуться к HTTP (не будет работать на телефоне):

1. Измените в `server.js`:
```javascript
const { createServer } = require('http');  // было: 'https'

// Закомментируйте:
// const httpsOptions = { ... };

// Измените:
const httpServer = createServer(async (req, res) => { ... });  // без httpsOptions
```

2. Обновите адреса на `http://` вместо `https://`

## 🐛 Устранение неполадок

### На телефоне не появляется запрос доступа к камере
✅ Убедитесь, что используете **HTTPS** (не HTTP)
✅ Проверьте, что приняли сертификат (см. Шаг 3)
✅ Попробуйте обновить страницу (Ctrl+R)
✅ Проверьте настройки разрешений в браузере

### Не могу принять сертификат на iOS
- Попробуйте Safari (лучшая поддержка на iOS)
- Убедитесь, что открываете именно `https://` адрес

### Соединение разрывается
- Проверьте, что порт 4057 открыт в файрволе
- Убедитесь, что DMZ настроен правильно

## ✅ Проверка

После всех шагов:
1. Откройте `https://192.168.50.57:4057/videocall` на телефоне
2. Примите сертификат
3. Разрешите доступ к камере/микрофону
4. Вы должны увидеть свое видео
5. Откройте на другом устройстве - устройства автоматически соединятся
